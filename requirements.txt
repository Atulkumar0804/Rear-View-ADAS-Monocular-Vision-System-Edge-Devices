torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
tqdm>=4.65.0
Pillow>=10.0.0
ultralytics>=8.0.0
flask>=3.0.0
transformers>=4.30.0
huggingface_hub>=0.16.0
timm>=0.9.0
scipy>=1.11.0

# Raspberry Pi camera support
# On Raspberry Pi OS, install Picamera2 with:
#   sudo apt install python3-picamera2
# The pip package is listed here for environments where it is available.
# picamera2; platform_machine == "aarch64" or platform_machine == "armv7l"

# Depth Pro - Apple's zero-shot metric depth model (no camera intrinsics needed)
# Install with: pip install git+https://github.com/apple/ml-depth-pro.git
# Or if you cloned the repo: pip install -e /path/to/ml-depth-pro
